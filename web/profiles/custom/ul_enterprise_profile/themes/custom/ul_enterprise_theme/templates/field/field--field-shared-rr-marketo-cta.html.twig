{% for item in items %}
    {% include '@ul_base_theme/2_pieces/siderail--cta.twig' with {
        color: 'button--red',
        class: item.content['#attributes'].class|join(' '),
        link: item.content['#url'],
        name: item.content['#settings']['button_text'] ? item.content['#settings']['button_text'] : item.content['#title']
    } %}
{% endfor %}
