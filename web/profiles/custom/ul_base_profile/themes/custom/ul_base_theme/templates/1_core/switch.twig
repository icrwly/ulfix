{% extends switch_template %}

{#
    This template is solely designed to be extended.
    All extensions should use logic to set the "switch_template" variable.
    The purpose is to encapsulate switching logic outside of the template itself.
#}
