# This is a placeholder file used to track when example.local.aliases.drushrc.php was converted.
# If you delete example.local.aliases.drushrc.php, then you may delete this file.com:
base:
  uri: http://base.${env.VIRTUAL_HOST}
  root: /var/www/docroot
enterprise:
  uri: http://enterprise.${env.VIRTUAL_HOST}
  root: /var/www/docroot
guidelines:
  uri: http://guidelines.${env.VIRTUAL_HOST}
  root: /var/www/docroot
